node_modules/
dist/
.DS_Store
.env*
!.env.example
playwright-report/
test-results/
mystic_woods_free_2/
Desert Tileset 32x32/
SWAMP TILESET 32x32.png

.venv/
scripts/source-cache/
*.tsbuildinfo

__pycache__/
*.pyc

# Regenerable lossless soundtrack listening masters; compact previews are committed.
artifacts/audio/*.wav

# Scratch scripts run with tsx from the repo root.
*.tmp.ts

# Third-party tilesets kept locally for reference; extracted crops are committed.
tilesets for experimenting/

# Stray editor backups
*.bak
server/dist/

# Generated by npm run town:sheet.
artifacts/towns/

# --- Regenerable output and local reference material (untracked 2026-09-22) ---
# Review screenshots written by tests and capture scripts. Regenerate with
# `npm run shot`, `npm run test:graphics` or the relevant capture script.
# Data files under artifacts/ stay tracked: measure-roads.ts reads one.
artifacts/**/*.png
artifacts/**/*.jpg
artifacts/**/*.jpeg
artifacts/**/*.gif
artifacts/**/*.webp

# Headless/MCP session logs written by the test suite.
runs/

# Loose mockups and screenshots dropped at the repo root. The four that the
# build and docs actually use are already tracked and stay that way.
/*.png
/*.jpg
/*.jpeg

# Third-party and reference art kept locally, not used by the build.
character_modal_backgrounds/
Pixel Crawler - Free Pack/
Basic Asset Pack for Animals/
