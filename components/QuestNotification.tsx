/**
 * QuestNotification.tsx - Stubbed out (quest system removed)
 *
 * This component was part of the quest notification system which has been removed.
 * Keeping this stub to avoid breaking imports.
 */
import React from 'react';

const QuestNotificationToast: React.FC = () => {
  // Quest system removed - component no longer functional
  return null;
};

export default QuestNotificationToast;
